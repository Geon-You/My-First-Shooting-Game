#pragma once

// 이 곳에 프로그램에서 자주 사용되는 헤더파일을 미리 불러놓음
// -> 결과적으로 해당 헤더 파일만 부른다면 사용할 여러 개의 헤더를
//    한 번에 불러올 수 있음 (생산성 향상)

#include <iostream>
#include <Windows.h>
#include <conio.h>
#include <string.h>
#include <string.h>
#include <list>
using namespace std;

#include "Define.h"